package Unit.src.map;

import map.Vertex;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class vertexTest {
    /*
    @Test
    public void testThatANodeCanBeGreaterThenAnother(){
        Vertex v1 = new Vertex("cucina",true,2);
        Vertex v2 = new Vertex("salone",true ,1);
        int result = v1.compareTo(v2);
        assertEquals(result,-1);
    }

    @Test
    public void testThatANodeCanBeEqualThenAnother(){
        Vertex v1 = new Vertex("cucina",true,2);
        Vertex v2 = new Vertex("salone",true ,2);
        int result = v1.compareTo(v2);
        assertEquals(result,0);
    }

    @Test
    public void testThatANodeCanBeSmallerThenAnother(){
        Vertex v1 = new Vertex("cucina",true,1);
        Vertex v2 = new Vertex("salone",true ,2);
        int result = v1.compareTo(v2);
        assertEquals(result,1);
    }
    */
}
